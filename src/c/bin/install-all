#!/bin/sh
set -e
home=$(realpath "$0")
home=${home%/*/*}
cd $home
$HOME/src/asm/bin/install-all
lib/bin/install
bin/install abspath -lcore -lsys
#bin/install cargo-foreach -lcore -lsys
bin/install en% -lcore -lsys
bin/install fsync -lcore -lsys
bin/install printenv -lcore -lsys
bin/install realpath -lcore -lsys
bin/install un% -lcore -lsys
