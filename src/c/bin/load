#!/bin/sh
set -e
home=$(realpath "$0")
home=${home%/*/*}
cd $home
main=$1
shift
test -e $main.o -a $main.c -nt $main.o && rm -f $main.o
test -e $main.o || bin/compile $main.c
tcc -L$HOME/lib -o $main $main.o "$@"
