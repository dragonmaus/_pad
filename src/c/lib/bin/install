#!/bin/sh
set -e
home=$(realpath "$0")
home=${home%/*/*}
cd $home
bin/generate error.h
mkdir -p $HOME/inc
cp -f *.h $HOME/inc
bin/generate error.c
bin/generate error_str.c
bin/generate error_temp.c
bin/compile *.c
bin/makelib libcore.a *.o
mkdir -p $HOME/lib
cp -f *.a $HOME/lib
