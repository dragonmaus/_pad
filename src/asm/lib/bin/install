#!/bin/sh
set -e
home=$(realpath "$0")
home=${home%/*/*}
cd $home
mkdir -p $HOME/inc
cp -f *.h $HOME/inc
for f in *.s
do
  bin/assemble $f
done
bin/makelib libsys.a *.o
mkdir -p $HOME/lib
cp -f *.a $HOME/lib
